// import { decodedToken, isExpired, reEvaluateToken } from 'react-jwt'

// export const isExpired = async ( token ) =>{
//   var ud = decode(token,{complete:true});
//   console.log(ud);
//   return isError;
//   }

// export const refresh = ( token ) => {
//   var ud = jwt.decode(token,{complete:true});
//   var userId = ud.payload.id;
//   var firstName = ud.payload.firstName;
//   var lastName = ud.payload.lastName;
//   var email = ud.payload.email;
//   var favorites = ud.payload.favorites;
//   return _createToken( firstName, lastName, userId, email, favorites);
// }

// export const decodeToken = ( token ) =>{
//   return jwt.decode(token,{complete:true});
// }